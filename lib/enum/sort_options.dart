enum SortOption {
  ratingDesc,     // По рейтингу, высокий → низкий
  priceAsc,       // По цене, низкая → высокая
  availability,   // По доступности (сначала доступные)
  reviewsDesc,    // По количеству отзывов, много → мало
}